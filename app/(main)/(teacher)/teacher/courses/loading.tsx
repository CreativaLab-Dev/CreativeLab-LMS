import ResourceTeacherPageSkeleton from "@/components/ui/sckeleton-teacher";

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <ResourceTeacherPageSkeleton />;
}